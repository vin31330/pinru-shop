# V23.7 四項整合修改操作說明

本次包含 4 個項目：

1. Banner 在手機 / 平板 / 電腦版一起縮小，並加上「往下滑看更多商品」提示。
2. 按摩系列商品可用「活動商品 88 折」方式設定。
3. 加入網站圖示（Favicon / Apple Icon / Manifest Icon）。
4. 綁定 Google Analytics（GA4）。

---

## 一、本次已修改的程式檔案

- `src/app/layout.tsx`
- `src/components/Banner.tsx`
- `src/components/GoogleAnalytics.tsx` ← 新增
- `src/components/QuantityDiscountActivitySelector.tsx`
- `src/lib/activityPresentation.ts`
- `src/lib/cart.ts`
- `src/lib/promotionEngine.ts`

---

## 二、如何套用程式

### 方式 A：直接使用完整版（建議）
1. 先備份你現在正在使用的專案資料夾。
2. 解壓縮 `pinru-shop-V23.7-四項整合完整版.zip`。
3. 用解壓後的整個專案資料夾，直接覆蓋你目前本機的網站專案。
4. 進入專案資料夾後執行：
   - `npm install`
   - `npm run build`
5. 測試沒問題後再 commit / push。

### 方式 B：只覆蓋修改檔
1. 解壓縮 `pinru-shop-V23.7-四項整合覆蓋包.zip`。
2. 依照原本相同路徑，將裡面的檔案覆蓋到你現在專案內。
3. 執行：
   - `npm run build`
4. 測試沒問題後再 commit / push。

---

## 三、按摩系列商品 88 折的表格設定方式

這版不需要新增新工作表。
直接使用現有的 **Activities** + **Activity Products** 即可。

### A. Activities 表新增 1 筆活動

建議填法如下：

- `活動ID`：自己填唯一值，例如 `massage88`
- `活動名稱`：按摩系列商品全館 88 折
- `活動副標題`：按摩舒壓系列限時優惠
- `活動說明`：按摩系列商品限時 88 折，歡迎選購。
- `活動類型`：件數優惠
- `需要件數`：1
- `觸發件數`：1
- `優惠方式`：支付比例
- `優惠值`：88
- `優惠套用對象`：全部活動商品
- `優惠件序`：1
- `是否可重複套用`：TRUE
- `是否逐件選規格`：FALSE（若按摩商品沒有逐件規格）
- `首頁排序`：自行填數字
- `顯示狀態`：TRUE
- `活動開始日期` / `活動結束日期`：依需要填寫
- `活動圖片` 或 `網站活動圖片網址`：放活動圖

### B. Activity Products 表新增多筆活動商品

把所有按摩系列商品都加進同一個活動。

每一筆建議這樣填：

- `活動商品ID`：唯一值，例如 `massage88-001`
- `活動ID`：`massage88`
- `商品ID`：該按摩商品的商品ID
- `商品角色`：活動商品
- `顯示順序`：1、2、3…
- `是否允許重複選`：TRUE
- `每組最多選擇數`：可留白
- `活動商品價格`：留白
- `顯示狀態`：TRUE

### C. 顯示效果

前台會變成：
- 按摩活動頁列出所有活動商品。
- 客人可以用 `+ / -` 選擇各商品數量。
- 每一件都會自動以 **原價的 88%** 計算。

---

## 四、Favicon 已處理的內容

這次已經在 `layout.tsx` 補上：

- `favicon.ico`
- `icon-192.png`
- `icon-512.png`
- `apple-touch-icon.png`
- `site.webmanifest`

如果你之後要換圖示，只需要替換 `public/` 裡的這幾個檔案即可。

建議：
- `favicon.ico`：瀏覽器小圖示
- `icon-192.png`：Android / PWA
- `icon-512.png`：高解析度圖示
- `apple-touch-icon.png`：iPhone / iPad 加到主畫面

---

## 五、Google Analytics 綁定步驟（GA4）

### Step 1：建立 GA4
1. 打開 Google Analytics。
2. 建立帳戶 / 資源。
3. 新增資料串流，選擇「網站」。
4. 網址填你的網站網址。
5. 建立後會拿到一組 **Measurement ID**，格式像：
   - `G-XXXXXXXXXX`

### Step 2：在 Netlify 設定環境變數
1. 打開 Netlify。
2. 進入你的站台。
3. 左側點：`Site configuration` → `Environment variables`。
4. 新增變數：
   - Key：`NEXT_PUBLIC_GA_ID`
   - Value：你的 GA4 Measurement ID（例如 `G-XXXXXXXXXX`）
5. 儲存。

### Step 3：重新部署
因為環境變數有變更，所以要重新部署一次。

建議做法：
1. 本機先完成所有程式更新。
2. `Commit to main`
3. `Push origin`
4. 等 Netlify 自動部署完成。

### Step 4：驗證是否成功
1. 打開網站。
2. 回到 GA 後台。
3. 進入 `即時報表（Realtime）`。
4. 自己打開網站，應該就會看到 1 位使用者。

---

## 六、Banner 修改內容

這版已經處理：

- 手機版縮小 Banner 高度
- 平板版縮小 Banner 高度
- 電腦版縮小 Banner 高度
- 保留圖片完整顯示
- 在 Banner 下方加入：`往下滑看更多商品 ↓`

目的：
- 避免客人以為 Banner 就是整頁結束
- 更明確知道下面還有商品內容

---

## 七、建議上線流程

因為你前面有提到 Netlify 點數快用完，建議這次採用：

1. 先在本機全部覆蓋
2. 本機測試
3. 測試項目包含：
   - 首頁 Banner 高度是否正常
   - Favicon 是否有顯示
   - 按摩 88 折活動是否能正常加入購物車
   - GA 即時報表是否有資料
4. 確認都正常後，再一次 push 到 Netlify

這樣最省部署次數。
